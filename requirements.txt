# Core MT5 bridge
mt5linux>=1.0.3
# MT4 automation uses a file bridge (no extra deps). pyzmq only if the optional
# ZeroMQ transport is ever enabled (file transport is the default, Wine-safe path).
# pyzmq>=25.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0           # Parquet feature storage (Phase 3+)

# Config & env
pyyaml>=6.0
python-dotenv>=1.0.0

# AI / LLM
anthropic>=0.40.0

# ── Phase 4+: ML models ───────────────────────────────────────────────────────
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.0
joblib>=1.3

# ── Phase 6+: Neural nets ─────────────────────────────────────────────────────
# torch>=2.2              # uncomment when reaching Phase 6

# ── Phase 10: Production ──────────────────────────────────────────────────────
# shap>=0.44              # explainability
# fastapi>=0.110          # dashboard API
# uvicorn>=0.29           # dashboard server
