"""V5 validation and artifact helpers.

These helpers are deliberately separate from live trading code. V5 starts as an
audit and compatibility layer around the existing MT5 stack, not a runtime
replacement.
"""

